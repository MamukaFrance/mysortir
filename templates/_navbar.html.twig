<nav class="uk-navbar-container custom-navbar fixed-top uk-margin">

    <div class="uk-container">
        <div class="uk-navbar">

            <div class="uk-navbar-left">
                <a href="{{ path('main_home') }}">
                    <img src="{{ asset('img/logo.png') }}" alt="logo" class="logo">
                </a>
                <ul class="uk-navbar-nav">
                    <li class="uk-active"><a href="{{ path('main_home') }}">Accueil</a></li>
                    <li class="uk-active"><a href="{{ path('sortie_list') }}">Liste</a></li>
                    {% if app.user %}
                    <li class="uk-active uk-visible@m"><a href="{{ path('sortie_new') }}">Créer</a></li>
                    <li class="uk-active uk-visible@m"><a href="{{ path('app_groupe_prive_new') }}">Créer Goupe</a></li>
                    <li class="uk-active uk-visible@m"><a href="{{ path('app_groupe_prive_index') }}">Mes groupe</a></li>
                    {% endif %}
                     {% if is_granted('ROLE_ADMIN') %}
                        <li class="uk-active"><a href="{{ path('app_participant_index') }}">Liste des utilisateurs</a></li>
                         <li class="uk-active uk-visible@m"><a href="">CSV</a></li>
                     {% endif %}
                    <li class="uk-active"><a href="#">A propos</a></li>
                    {% if app.user %}
                    <li class="uk-active uk-visible@m"><a href="">Gerer les Villes et Lieux</a></li>
                    {% endif %}
                </ul>
            </div>

            <div class="uk-navbar-right uk-flex uk-flex-middle uk-gap-small">

                {% if app.user %}
                    <div>
                        <span>Bonjour {{ app.user.prenom }}</span>
                        {% if app.user %}
{#                            <img src="{{ asset('uploads/photos/' ~ app.user.photo) }}" alt="Photo de profil" class="navbar_photo_profil uk-border-circle">#}
                        {% else %}
                            <img src="{{ asset('img/profil_defaut.png') }}" alt="Photo de profil" class="navbar_photo_profil uk-border-circle">
                        {% endif %}
                    </div>
                    <div class="navbar_droite">
{#                        <a href="{{ path('app_participant_show') }}" class="uk-button uk-button-text">Mon profil</a>#}
                        <a href="{{ path('app_logout') }}" class="uk-button uk-button-text">Se déconnecter</a>
                    </div>
                {% else %}
                    <a href="{{ path('app_register') }}" class="uk-button uk-button-text">S'enregistrer</a>
                    <a href="{{ path('app_login') }}" class="uk-button uk-button-text">Se connecter</a>
                {% endif %}
            </div>

        </div>
    </div>
</nav>
